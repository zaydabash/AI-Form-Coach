# FormIQ backend dependencies (local server-webcam mode).
# MediaPipe requires numpy<2, so pin it to avoid the OpenCV/MediaPipe ABI
# mismatch you get with numpy 2.x.
numpy>=1.26,<2.0

# Web framework + server
fastapi>=0.110
uvicorn[standard]>=0.29

# Computer vision + pose. MediaPipe 0.10.35 uses the Tasks API
# (PoseLandmarker), which needs a pose_landmarker.task model file
# (see README, fetched at setup, not bundled).
opencv-python>=4.9
mediapipe>=0.10.35

# Coaching model client
anthropic>=0.40

# Text-to-speech via REST
requests>=2.31
