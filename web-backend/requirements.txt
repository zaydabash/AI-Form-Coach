# FormIQ cloud API dependencies. Slim: no OpenCV/MediaPipe, since the browser
# handles camera capture and pose tracking.
fastapi>=0.110
uvicorn[standard]>=0.29
anthropic>=0.40
requests>=2.31
pydantic>=2.0
